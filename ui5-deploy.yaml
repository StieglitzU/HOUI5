# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: at.cloudna.zhoui505
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: CloudDNA_WDF_FSD
          url: http://s4dev.virtual:8080
        app:
          name: ZHOUI5_05
          description: HOUI5 APP Student05
          package: ZTRAIN05
          transport: FSDK900139
